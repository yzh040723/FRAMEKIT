# FRAMEKIT Windows 打包操作手册

本文档适用于当前 FRAMEKIT Electron 项目，在 Windows PowerShell 环境下生成 NSIS 安装包。

## 一、环境要求

- Windows 10/11
- Node.js 和 npm 已安装
- 当前项目目录
- 项目依赖已安装，项目目录下存在 `node_modules`

如果依赖不存在，先执行：

```powershell
npm.cmd install
```

> 如果 PowerShell 提示禁止运行 `npm.ps1` 或 `npx.ps1`，请使用 `npm.cmd` 和 `npx.cmd`，不要直接使用 `npm` 或 `npx`。

## 二、关闭正在运行的程序

打包前建议关闭 FRAMEKIT，避免文件被占用：

```powershell
taskkill /F /IM FRAMEKIT.exe 2>$null
taskkill /F /IM electron.exe 2>$null
```

如果提示找不到进程，可以忽略。

## 三、编译 TypeScript 源码

进入项目目录：

执行编译：

```powershell
npm.cmd run build:ts
```

成功时会显示类似内容：

```text
> framekit@1.0.6 build:ts
> tsc
```

命令结束后返回 PowerShell 提示符且没有 `error TS...`，即表示编译成功。

`build:ts` 只负责生成 Electron 主进程编译文件，不会生成安装包。

## 四、生成安装包

推荐使用以下命令：

```powershell
npx.cmd electron-builder --win nsis --config.npmRebuild=false --publish never
```

参数说明：

- `--win nsis`：生成 Windows NSIS 安装包；
- `--config.npmRebuild=false`：跳过原生依赖重建，避免部分环境出现 `spawn EPERM`；
- `--publish never`：不执行发布或上传操作。

打包过程中第一次运行可能会下载 Electron Windows 运行包，例如：

```text
Downloading electron-v36.9.5-win32-x64.zip
```

这是正常现象，请等待下载和打包完成，不要关闭 PowerShell 或按 `Ctrl+C`。

## 五、如果出现证书或缓存权限问题

可以先设置当前 PowerShell 窗口使用的缓存目录：

```powershell
$env:ELECTRON_CACHE = ".\.electron-cache"
$env:NODE_USE_SYSTEM_CA = "1"

New-Item -ItemType Directory -Force ".electron-cache" | Out-Null
```

然后重新执行打包：

```powershell
npx.cmd electron-builder --win nsis --config.npmRebuild=false --publish never
```

如果仍然出现：

```text
unable to verify the first certificate
```

可以在当前窗口临时关闭 Node.js TLS 证书校验后重试：

```powershell
$env:NODE_TLS_REJECT_UNAUTHORIZED = "0"

npx.cmd electron-builder --win nsis --config.npmRebuild=false --publish never

Remove-Item Env:NODE_TLS_REJECT_UNAUTHORIZED -ErrorAction SilentlyContinue
```

> 仅在当前构建窗口临时使用该设置。打包完成后必须移除环境变量，或直接关闭该 PowerShell 窗口。不要将此设置写入系统环境变量。

如果出现：

```text
EPERM: operation not permitted
```

请尝试：

1. 关闭 FRAMEKIT 和 Electron 进程；
2. 以管理员身份打开 PowerShell；
3. 使用项目目录下的 `.electron-cache`；
4. 使用 `--config.npmRebuild=false` 重新打包。

## 六、一键打包命令

建议复制下面整段命令执行：

```powershell
Set-Location .

taskkill /F /IM FRAMEKIT.exe 2>$null
taskkill /F /IM electron.exe 2>$null

$env:ELECTRON_CACHE = ".\.electron-cache"
$env:NODE_USE_SYSTEM_CA = "1"

New-Item -ItemType Directory -Force ".electron-cache" | Out-Null

npm.cmd run build:ts

if ($LASTEXITCODE -eq 0) {
    npx.cmd electron-builder --win nsis --config.npmRebuild=false --publish never
}
```

如果只是 PowerShell 禁止脚本，不需要修改执行策略，直接使用上面的 `.cmd` 命令即可。

## 七、检查安装包是否更新

打包成功后执行：

```powershell
Get-Item .\release\FRAMEKIT-Setup-*.exe |
  Select-Object Name, Length, LastWriteTime
```

当前安装包路径为：

```text
.\release\FRAMEKIT-Setup-1.0.6.exe
```

重点检查：

- `LastWriteTime` 是否为本次打包时间；
- 文件大小是否大于 0；
- 是否同时生成 `.blockmap` 和 `latest.yml` 文件。

也可以检查解包目录：

```powershell
Get-Item .\release\win-unpacked\resources\app.asar |
  Select-Object Name, Length, LastWriteTime
```

## 八、安装和验证

双击以下文件安装：

```text
.\release\FRAMEKIT-Setup-1.0.6.exe
```

安装后重点验证：

1. 启动动画是否为新的 Camera Lens Animation；
2. 镜头动画是否位于窗口中心；
3. 启动动画是否正常淡出进入主页；
4. 明亮主题下工具网址卡片悬停是否不再变黑；
5. 暗色主题下 EXIF 编辑器和工具网址模块的遮罩是否一致；
6. EXIF 编辑、地图定位和导出功能是否正常。

## 九、常用命令对照

| 操作 | 命令 |
|---|---|
| 安装依赖 | `npm.cmd install` |
| 编译 TypeScript | `npm.cmd run build:ts` |
| 启动开发版 | `npm.cmd run dev` |
| 生成安装包 | `npx.cmd electron-builder --win nsis --config.npmRebuild=false --publish never` |
| 生成目录版 | `npm.cmd run pack` |
|
## 十、注意事项

- 修改 `web/index.html`、`web/style.css` 或 `web/app.js` 后，必须重新执行 `npm.cmd run build:ts` 和 Electron 打包命令，安装包才会包含最新内容；
- 仅执行 `npm.cmd run build:ts` 不会更新安装包；
- 安装包文件名可能保持为 `FRAMEKIT-Setup-1.0.6.exe`，应通过 `LastWriteTime` 判断是否为新文件；
- 如果旧版本正在运行，安装包可能无法覆盖部分文件，请先退出旧版本；
- `.electron-cache` 可以保留，后续打包可减少重复下载；
- 不要把 `NODE_TLS_REJECT_UNAUTHORIZED=0` 固化到系统环境变量中。
